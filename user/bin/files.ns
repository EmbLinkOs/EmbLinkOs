# Files inherits the authenticated session namespace.
#
# This is intentional: a user's file manager must see exactly what that user
# can see (their writable home plus the read-only system/application trees).
# Asking for `rw /` here would exceed the session's authority and the kernel
# would correctly reject the spawn with -ENOENT.
