# files namespace manifest (docs/USERSPACE_v2.md UP4).
# The file manager browses the WHOLE tree, so its broad view is a real
# privilege -- but here it is EXPLICITLY declared, not ambient, and the OS stays
# sealed: /system is still read-only. This is the "explicitly-broad-view" app.
rw /
ro /system
