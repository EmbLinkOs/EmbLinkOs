# framepace namespace manifest (docs/USERSPACE_v2.md UP4).
# It draws from the UI runtime under the sealed, read-only /system and owns a
# window. No file I/O of its own, so it cannot name /data, /run, or a home --
# the same narrow view as uidemo, for the same reason.
ro /system
