#ifndef EMBK_TLS_KSHIM_TYPES_H
#define EMBK_TLS_KSHIM_TYPES_H
/* Userspace/host shim for the kernel's include/types.h, so the same pure-C
 * crypto (kernel/crypto .c) compiles here against standard headers. */
#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>
#endif
